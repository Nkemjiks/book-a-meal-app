export default class Meal {
  constructor(id, name, price, image, isChecked) {
    this.id = id;
    this.name = name;
    this.price = price;
    this.image = image;
    this.isChecked = isChecked;
  }
}
